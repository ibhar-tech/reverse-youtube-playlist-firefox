# Packaged build artifact (built from source; attach to GitHub Releases instead)
reverse-playlist.zip
*.zip

# API credentials — never commit these
.env

# Python bytecode from the e2e harness
__pycache__/
*.pyc
